# Optional SQL Server connection string for direct Save to Database.
PSX_SQL_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=PSXFinancials;Trusted_Connection=yes;TrustServerCertificate=yes;

# Extraction/runtime settings
PSX_CASE_TIMEOUT_SECONDS=420
PSX_ENABLE_HEAVY_OCR=0
PSX_LAYOUT_FALLBACK_SCORE=25
